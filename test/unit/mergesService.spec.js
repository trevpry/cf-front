'use strict';

describe('grid merges service', function(){
  var mergesService;
  var data = {};

  //beforeEach(module('cfFront', ['ui.router', 'mediaPlayer', 'ngAnimate', 'ui.bootstrap']));

  //beforeEach(inject(function(_mergesService_) {
  //	mergesService = _mergesService_;
  //}));
  //
  //it('should return an array of merges', function() {
  //
  //  var merges = mergesService.getMerges(data);
  //  expect(angular.isArray(merges)).toBeTruthy();
  //});
});
